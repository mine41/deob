# Case100 Summary

## Overall Statistics

- Total samples: `100`
- Benign samples: `50`
- Malicious samples: `50`

| Tool | Valid | Correct |
| --- | ---: | ---: |
| PSDissect | 100 / 100 | 98 / 100 |
| PowerPeeler | 95 / 100 | 83 / 100 |
| IVD | 100 / 100 | 62 / 100 |

## Per-Sample Status

`valid` means the output is syntactically correct and not identical to the obfuscated input; `correct` means the output also recovers the intent of the matching script in `originals`.

| Sample | Source | PowerPeeler | IVD | PSDissect | Reason |
| --- | --- | --- | --- | --- | --- |
| `benign-ob-1.ps1` | `benign` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `benign-ob-2.ps1` | `benign` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `benign-ob-3.ps1` | `benign` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `benign-ob-4.ps1` | `benign` | `valid-only` | `valid-only` | `correct` | PowerPeeler still leaves cmdlet heads obfuscated at benign-powerpeeler-4.ps1:3 "neW-I\`T\`em" and :6 "Ge\`T-cHiLDIt\`em", IVD corrupts them into broken forms at benign-ivd-4.ps1:2-3 "t...st-path" and "new-it...m", while PSDissect restores direct cmdlets at benign-psdissect-4.ps1:2 and :7. |
| `benign-ob-5.ps1` | `benign` | `valid-only` | `valid-only` | `correct` | PowerPeeler still calls format-built command heads at benign-powerpeeler-5.ps1:2 and :6 instead of plain "Test-Path" and "Get-ChildItem", IVD keeps the same indirection and also corrupts names at benign-ivd-5.ps1:7 "${fiL...s}" and :13 "${inV...ORYpAtH}", while PSDissect exposes direct cmdlets at benign-psdissect-5.ps1:1 and :7. |
| `benign-ob-6.ps1` | `benign` | `valid-only` | `valid-only` | `correct` | PowerPeeler still leaves backtick-split tokens like benign-powerpeeler-6.ps1:1 "J\`o\`In-PATh" and :6 "g\`e\`T-chiLDI\`Tem", IVD turns cmdlets into broken forms at benign-ivd-6.ps1:2 "t...st-path" and :8 "wh...re-object", while PSDissect normalizes them to plain "Join-Path" and "Get-ChildItem" at benign-psdissect-6.ps1:1 and :7. |
| `benign-ob-7.ps1` | `benign` | `valid-only` | `valid-only` | `correct` | PowerPeeler keeps indirect command construction at benign-powerpeeler-7.ps1:9 and :11 for "Get-ChildItem" and "Get-FileHash", IVD also leaves corrupted names at benign-ivd-7.ps1:16 "${HASH...s}", while PSDissect emits direct steps at benign-psdissect-7.ps1:11 and :13. |
| `benign-ob-8.ps1` | `benign` | `valid-only` | `valid-only` | `correct` | PowerPeeler still preserves lexical obfuscation such as benign-powerpeeler-8.ps1:4 "D\`IrecT\`oRy" and :11 "S\`ha2\`56", IVD carries the same obfuscated tokens and corrupts the final variable at benign-ivd-8.ps1:16 "${hAsh...S}", while PSDissect exposes the direct hash workflow at benign-psdissect-8.ps1:11-14. |
| `benign-ob-9.ps1` | `benign` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `benign-ob-10.ps1` | `benign` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `benign-ob-11.ps1` | `benign` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `benign-ob-12.ps1` | `benign` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `benign-ob-13.ps1` | `benign` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `benign-ob-14.ps1` | `benign` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `benign-ob-15.ps1` | `benign` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `benign-ob-16.ps1` | `benign` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `benign-ob-17.ps1` | `benign` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `benign-ob-18.ps1` | `benign` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `benign-ob-19.ps1` | `benign` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `benign-ob-20.ps1` | `benign` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `benign-ob-21.ps1` | `benign` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `benign-ob-22.ps1` | `benign` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `benign-ob-23.ps1` | `benign` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `benign-ob-24.ps1` | `benign` | `invalid` | `valid-only` | `correct` | PowerPeeler is treated as invalid because benign-powerpeeler-24.ps1 still collapses to the original string-rebuild shell and only resolves it through the line 21 execute-the-string tail, IVD keeps the same shell form at benign-ivd-24.ps1:21, while PSDissect reconstructs the real settings script at benign-psdissect-24.ps1:1-20. |
| `benign-ob-25.ps1` | `benign` | `invalid` | `valid-only` | `correct` | PowerPeeler is treated as invalid because benign-powerpeeler-25.ps1 is still a giant quoted wrapper from line 1 and only resolves it at line 21, IVD keeps the same wrapper at benign-ivd-25.ps1:1-21, while PSDissect restores the real app-settings workflow at benign-psdissect-25.ps1:1-20. |
| `benign-ob-26.ps1` | `benign` | `invalid` | `valid-only` | `correct` | PowerPeeler is treated as invalid because benign-powerpeeler-26.ps1 still begins with the obfuscated launcher at line 1 and never reduces to plain notes logic before the final Replace chain at line 24, IVD keeps the same launcher at benign-ivd-26.ps1:1-24, while PSDissect restores the searchable notes script at benign-psdissect-26.ps1:17-23. |
| `benign-ob-27.ps1` | `benign` | `invalid` | `valid-only` | `correct` | PowerPeeler is treated as invalid because benign-powerpeeler-27.ps1 still starts with "& ((VArIAbLE '*mDR*').NAME...)" at line 1 and only unwinds through a final cReplace chain at line 18, IVD keeps the same wrapper at benign-ivd-27.ps1:1-18, while PSDissect restores the direct greeting script at benign-psdissect-27.ps1:6-17. |
| `benign-ob-28.ps1` | `benign` | `correct` | `valid-only` | `correct` | IVD still corrupts the content pipeline at benign-ivd-28.ps1:4 "s...t-cont...nt" and :5 "g...t-content" and "s...lect-object", whereas PowerPeeler and PSDissect already match the plain workflow at benign-powerpeeler-28.ps1:4-5 and benign-psdissect-28.ps1:4-5. |
| `benign-ob-29.ps1` | `benign` | `correct` | `valid-only` | `correct` | IVD still damages core cmdlets at benign-ivd-29.ps1:2 "new-it...m", :4 "s...t-cont...nt", and :5 "sel...ct-object", whereas PowerPeeler and PSDissect keep the direct build-steps script at benign-powerpeeler-29.ps1:1-5 and benign-psdissect-29.ps1:1-5. |
| `benign-ob-30.ps1` | `benign` | `correct` | `valid-only` | `correct` | IVD still emits broken cmdlets at benign-ivd-30.ps1:2 "n...w-item", :4 "set-cont...nt", and :5 "sel...ct-object", whereas PowerPeeler and PSDissect reduce the sample to the same plain build-steps logic at benign-powerpeeler-30.ps1:1-5 and benign-psdissect-30.ps1:1-5. |
| `benign-ob-31.ps1` | `benign` | `correct` | `valid-only` | `correct` | IVD still corrupts the log pipeline at benign-ivd-31.ps1:2 "n...w-item" and :5 "get-cont...nt" and "sel...ct-object", whereas PowerPeeler and PSDissect already expose the direct recent.log workflow at benign-powerpeeler-31.ps1:4-5 and benign-psdissect-31.ps1:4-5. |
| `benign-ob-32.ps1` | `benign` | `correct` | `valid-only` | `correct` | IVD leaves broken selection/output tokens at benign-ivd-32.ps1:5 "s...lect-obj...ct" and :7 "writ...-output", whereas PowerPeeler and PSDissect preserve the plain log-trimming logic at benign-powerpeeler-32.ps1:4-7 and benign-psdissect-32.ps1:4-7. |
| `benign-ob-33.ps1` | `benign` | `correct` | `valid-only` | `correct` | IVD corrupts both the path variables and cmdlets at benign-ivd-33.ps1:4 "${PREVI...WpAth}", :5 "${qu...UEpATh}", and :6 "g...t-content" and "s...lect-object", whereas PowerPeeler and PSDissect keep the direct queue-preview script at benign-powerpeeler-33.ps1:3-6 and benign-psdissect-33.ps1:3-6. |
| `benign-ob-34.ps1` | `benign` | `correct` | `valid-only` | `correct` | IVD still breaks the queue-preview pipeline at benign-ivd-34.ps1:2 "n...w-item" and :6 "get-cont...nt", "s...lect-object", and "s...t-content", whereas PowerPeeler and PSDissect already match the original first-two-items logic at benign-powerpeeler-34.ps1:5-6 and benign-psdissect-34.ps1:5-6. |
| `benign-ob-35.ps1` | `benign` | `correct` | `valid-only` | `correct` | IVD still corrupts the release-preview path at benign-ivd-35.ps1:6 "sel...ct-object" and "${PrEvI...WPATh}", whereas PowerPeeler and PSDissect keep the direct three-notes script at benign-powerpeeler-35.ps1:5-6 and benign-psdissect-35.ps1:5-6. |
| `benign-ob-36.ps1` | `benign` | `correct` | `valid-only` | `correct` | IVD still leaves broken cmdlets at benign-ivd-36.ps1:2 "new-it...m", :5 "s...t-content", and :6 "s...lect-obj...ct", whereas PowerPeeler and PSDissect reduce the sample to the plain release-preview workflow at benign-powerpeeler-36.ps1:5-6 and benign-psdissect-36.ps1:5-6. |
| `benign-ob-37.ps1` | `benign` | `correct` | `valid-only` | `correct` | IVD still emits corrupted content cmdlets at benign-ivd-37.ps1:5 "set-cont...nt" and :6 "get-cont...nt" and "s...lect-object", whereas PowerPeeler and PSDissect already expose the direct preview generation script at benign-powerpeeler-37.ps1:5-6 and benign-psdissect-37.ps1:5-6. |
| `benign-ob-38.ps1` | `benign` | `correct` | `valid-only` | `correct` | IVD changes the JSON schema keys to lowercase at benign-ivd-38.ps1:4-6 "channel", "level", and "quiethours", and also corrupts "Set-Content" and "ConvertFrom-Json" at lines 6-7, whereas PowerPeeler and PSDissect preserve the original "Channel", "Level", and "QuietHours" keys. |
| `benign-ob-39.ps1` | `benign` | `correct` | `valid-only` | `correct` | IVD again lowercases the intended JSON keys at benign-ivd-39.ps1:4-6 and corrupts conversion cmdlets at lines 6-7 "conv...rtto-json" and "conv...rtfrom-json", whereas PowerPeeler and PSDissect keep the original "Channel/Level/QuietHours" structure. |
| `benign-ob-40.ps1` | `benign` | `correct` | `valid-only` | `correct` | IVD lowercases "Channel", "Level", and "QuietHours" at benign-ivd-40.ps1:4-6 and breaks the load path at line 7 "g...t-content" and "conv...rtfrom-json", whereas PowerPeeler and PSDissect remain faithful to the original alert-rules script. |
| `benign-ob-41.ps1` | `benign` | `correct` | `valid-only` | `correct` | IVD changes the ordered JSON keys to lowercase at benign-ivd-41.ps1:4-6 "app", "theme", and "refreshminutes" and also corrupts the loader at line 7, whereas PowerPeeler and PSDissect preserve the original "App", "Theme", and "RefreshMinutes" fields. |
| `benign-ob-42.ps1` | `benign` | `correct` | `valid-only` | `correct` | IVD lowercases the theme-profile keys at benign-ivd-42.ps1:4-6 "accent", "font", and "density" and corrupts both output lines at 8-9, whereas PowerPeeler and PSDissect keep the original "Accent", "Font", and "Density" semantics. |
| `benign-ob-43.ps1` | `benign` | `correct` | `valid-only` | `correct` | IVD again lowers the JSON keys at benign-ivd-43.ps1:4-6 and breaks both storage and loading tokens at lines 6-7 "s...t-content" and "conv...rtfrom-json", whereas PowerPeeler and PSDissect keep the original theme-profile logic. |
| `benign-ob-44.ps1` | `benign` | `correct` | `valid-only` | `correct` | IVD lowercases the theme-profile keys at benign-ivd-44.ps1:4-6 and still uses broken conversion/output cmdlets at line 6 "conv...rtto-json" and "set-cont...nt", whereas PowerPeeler and PSDissect match the original structure. |
| `benign-ob-45.ps1` | `benign` | `correct` | `valid-only` | `correct` | IVD changes the window-layout keys to lowercase at benign-ivd-45.ps1:4-6 "view", "sidebar", and "zoom" and corrupts the loader at line 7, whereas PowerPeeler and PSDissect preserve the original "View", "Sidebar", and "Zoom" fields. |
| `benign-ob-46.ps1` | `benign` | `correct` | `valid-only` | `correct` | IVD again lowercases the window-layout keys at benign-ivd-46.ps1:4-6 and breaks both JSON conversion calls at lines 6-7 "conv...rtto-json" and "conv...rtfrom-json", whereas PowerPeeler and PSDissect keep the direct original logic. |
| `benign-ob-47.ps1` | `benign` | `correct` | `valid-only` | `correct` | IVD lowercases the window-layout keys at benign-ivd-47.ps1:4-6 and still emits broken item/content cmdlets at lines 2 and 6, whereas PowerPeeler and PSDissect retain the original "View/Sidebar/Zoom" behavior. |
| `benign-ob-48.ps1` | `benign` | `invalid` | `valid-only` | `correct` | PowerPeeler is treated as invalid because benign-powerpeeler-48.ps1:1-2 still only rebuilds "Write-Host 'status-ok'" as a quoted string and dispatches it through a resolver, IVD keeps the same execute-the-string form at benign-ivd-48.ps1:1-2, while PSDissect exposes the direct command at benign-psdissect-48.ps1:1. |
| `benign-ob-49.ps1` | `benign` | `valid-only` | `valid-only` | `valid-only` | PowerPeeler still keeps the string-builder form at benign-powerpeeler-49.ps1:1 and IVD still keeps a quoted property name at benign-ivd-49.ps1:1, while PSDissect over-deobfuscates benign-original-49.ps1:1 "('tiny note').Length" into the fully evaluated literal benign-psdissect-49.ps1:1 "9", losing the original script expression. |
| `benign-ob-50.ps1` | `benign` | `valid-only` | `valid-only` | `valid-only` | PowerPeeler still keeps the string-builder form at benign-powerpeeler-50.ps1:1 and IVD still keeps a quoted property name at benign-ivd-50.ps1:1, while PSDissect over-deobfuscates benign-original-50.ps1:1 "('tiny note').Length" into the fully evaluated literal benign-psdissect-50.ps1:1 "9", losing the original script expression. |
| `malicious-ob-1.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-2.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-3.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-4.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-5.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-6.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-7.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-8.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-9.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-10.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-11.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-12.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-13.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-14.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-15.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-16.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-17.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-18.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-19.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-20.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-21.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-22.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-23.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-24.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-25.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-26.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-27.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-28.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-29.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-30.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-31.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-32.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-33.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-34.ps1` | `malicious` | `valid-only` | `valid-only` | `correct` | PowerPeeler still leaves the function/type names obfuscated at malicious-powerpeeler-34.ps1:1 "D\`O\`WNlOAd-..." and :13 "('N'+'e'+'t.Web'+'C'+'lient')", IVD also keeps the obfuscated function name and flattens the embedded script into single-quoted strings at malicious-ivd-34.ps1:23-28, while PSDissect restores direct forms such as malicious-psdissect-34.ps1:17 "New-Object 'Net.WebClient'" and :61 "Download-Execute-PS". |
| `malicious-ob-35.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-36.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-37.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-38.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-39.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-40.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-41.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-42.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-43.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-44.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-45.ps1` | `malicious` | `correct` | `valid-only` | `correct` | IVD never unwraps the XOR decoder and still ends as a rebuilt string executed by malicious-ivd-45.ps1:1-3, whereas PowerPeeler and PSDissect both expose the downloader stub directly from line 1 "function Download-Execute-PS". |
| `malicious-ob-46.ps1` | `malicious` | `valid-only` | `valid-only` | `correct` | PowerPeeler and IVD stop at the branch decoder and only rebuild "$scriptText" at malicious-powerpeeler-46.ps1:91-97 and malicious-ivd-46.ps1:85-91 before "Invoke-Expression", whereas PSDissect inlines the recovered payload body starting at malicious-psdissect-46.ps1:91 "function Invoke-PowerShellTcp". |
| `malicious-ob-47.ps1` | `malicious` | `valid-only` | `valid-only` | `correct` | PowerPeeler and IVD still stop at the statebag wrapper and end with "$scriptText" plus "Invoke-Expression" at malicious-powerpeeler-47.ps1:54 and malicious-ivd-47.ps1:55, whereas PSDissect exposes the real payload at malicious-psdissect-47.ps1:55 "function Invoke-SSIDExfil" with the original ROT13 and netsh logic below it. |
| `malicious-ob-48.ps1` | `malicious` | `correct` | `correct` | `correct` | All three tools produce a syntactically valid rewrite that departs from the obfuscated input and recovers the original script intent. |
| `malicious-ob-49.ps1` | `malicious` | `valid-only` | `valid-only` | `correct` | PowerPeeler and IVD still end at the staged decoder with "$scriptText" and "Invoke-Expression" at malicious-powerpeeler-49.ps1:40-46 and malicious-ivd-49.ps1:48-55, whereas PSDissect embeds the recovered "Invoke-PoshRatHttp" body at malicious-psdissect-49.ps1:56-117. |
| `malicious-ob-50.ps1` | `malicious` | `valid-only` | `valid-only` | `correct` | PowerPeeler and IVD stop at the branch wrapper and only execute rebuilt text at malicious-powerpeeler-50.ps1:72-78 and malicious-ivd-50.ps1:85-91, whereas PSDissect exposes the recovered payload function at malicious-psdissect-50.ps1:91 "function Invoke-SSIDExfil". |
